import './Loading.css';

const Loading = ({ next, prev }) => {
    return <>
        <div class="wrapper">
            <div class="pokeball">
            </div>
        </div>
    </>
}

export default Loading;